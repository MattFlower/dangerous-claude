# Additional apt packages to install
# One package per line. Lines starting with # are comments.
#
# These are installed during 'dangerous-claude build'
# Copy this file to 'packages.apt' and customize as needed.

# Editors
vim
nano

# Utilities
tree
htop

# Python (useful for many tools and scripts)
python3
python3-pip

# Build tools (needed for some npm packages with native dependencies)
build-essential
